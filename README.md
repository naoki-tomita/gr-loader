# gr-loader

the RICOH GR photo load cli command.

gr-loader executes ...

1. connect to RICOH GR.
1. create destination directory.
1. load photos.

# getting started

```shell
> npm i -g gr-loader
> gr-loader path/to/destination/dir # directory will be created if not exist.
```
